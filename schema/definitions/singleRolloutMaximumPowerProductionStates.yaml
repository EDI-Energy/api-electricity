components:
  schemas:
      singleRolloutMaximumPowerProductionStates:
        type: array
        minItems: 0
        description: "Array zur Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen max. Erzeugungsleistung."
        x-Translation:
          de: "Array zur Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen max. Erzeugungsleistung."
        items:
          $ref: './singleRolloutMaximumPower.yaml#/components/schemas/singleRolloutMaximumPower'