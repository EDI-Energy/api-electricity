components:
  schemas:
    annuallyRolloutSwitchingTime:
      required:
      - code
      - executionTimeFrom
      - executionTimeUntil
      - version
      - startPowerStateBeginYear
      - annuallyRolloutSwitchingTimes
      type: object
      description: "Jährlich ausgerollte Schaltzeit"
      x-Translation:
        de: "Jährlich ausgerollte Schaltzeit"
      properties:
        code:
          $ref: './../codeAn3.yaml#/components/schemas/codeAn3'
          description: "Code der Schaltzeit"
        executionTimeFrom:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
          description: "Zeitpunkt Gültig Ab (01.01 00:00 Uhr eines Kalenderjahres)"
        executionTimeUntil:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
          description: "Zeitpunkt Gültig Bis (01.01 00:00 Uhr eines Kalenderjahres)"
        version:
          $ref: './../dateAndTime/dateTime.yaml#/components/schemas/dateTime'
          description: "Versionsangabe"
        orderReference:
          $ref: './../header/transactionId.yaml#/components/schemas/transactionId'
          description: "Referenz auf eine mögliche Reklamation"
        startPowerStateBeginYear:
          $ref: './switchingTimeState.yaml#/components/schemas/switchingTimeState'
          description: "Der Status der Leistung an der Lokation zum 01.01. eines Kalenderjahres."
        annuallyRolloutSwitchingTimes:
          $ref: './annuallyRolloutSwitchingTimeStates.yaml#/components/schemas/annuallyRolloutSwitchingTimeStates'
          description: "Das zum 01.01. des jeweiligen Kalenderjahres aktive Register"
         
