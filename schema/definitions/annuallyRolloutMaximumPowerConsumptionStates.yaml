components:
  schemas:
      annuallyRolloutMaximumPowerConsumptionStates:
        type: array
        minItems: 1
        description: "Array zur Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen max. Bezugsleistung."
        x-Translation:
          de: "Array zur Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen max. Bezugsleistung."
        items:
          $ref: './annuallyRolloutMaximumPower.yaml#/components/schemas/annuallyRolloutMaximumPower'