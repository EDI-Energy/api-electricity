components:
  schemas:
    singleRolloutMaximumPower:
      required:
      - executionTimeFrom
      - maxPower
      type: object
      description: "Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen max. Leistung."
      x-Translation:
        de: "Angabe eines Zeitpunktes und den zu diesem Zeitpunkt vorhandenen max. Leistung."
      properties:
        executionTimeFrom:
          $ref: './../dateAndTime/hourAndMinute.yaml#/components/schemas/hourAndMinute'
        maxPower:
          $ref: './maximumPower.yaml#/components/schemas/maximumPower'