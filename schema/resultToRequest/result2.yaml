components:
  schemas:
    result2:
      description: "Hier wird bei einer Antwort der Entscheidungsbaum und der entsprechende Antwortcode aus dem Entscheidungsbaumdiagramm angegeben. Ein Freitext ist nur dann anzugeben, wenn der Antwortcode des entsprechenden Entscheidungsbaumdiagramms die Angabe eines Freitextes vorsieht."
      x-Translation:
        de: "Ergebnis"
      required:
      - responseCode
      - decisionTree
      type: object
      properties:
        decisionTree:
          $ref: './decisionTree.yaml#/components/schemas/decisionTree'
        responseCode:
          $ref: './responseCode.yaml#/components/schemas/responseCode'
        reason:
          $ref: './../freeText.yaml#/components/schemas/freeText'