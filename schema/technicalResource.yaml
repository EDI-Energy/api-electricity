components:
  schemas:
    technicalResource:
      required:
      - trId
      type: object
      x-Translation:
        de: "Technsiche Ressource"
      properties:
        trId:
          $ref: './trId.yaml#/components/schemas/trId'
 