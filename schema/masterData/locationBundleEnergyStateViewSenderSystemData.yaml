components:
  schemas:
    locationBundleEnergyStateViewSenderSystemData:
      type: string
      description: "Lokationsbündel Energy Sichtweise des Senders. systemData=im System vorhandene Daten, noSystemData= keine Daten im System vorhanden"
      example: "systemData"
      enum:
      - systemData