components:
  schemas:
    locationBundleEnergyStateViewSenderExpectedData:
      type: string
      description: "Lokationsbündel Energy Sichtweise des Senders. noData=keine Daten erwartet, expectedData=erwartete Daten."
      example: "expectedData"
      enum:
      - expectedData
