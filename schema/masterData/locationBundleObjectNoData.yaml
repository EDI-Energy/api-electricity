components:
  schemas:
    locationBundleObjectNoData:
      required:
      - executionDateTime
      type: object
      x-Translation:
        de: "Eine Zeitscheibe der Liste in der im Lokationsbündel keine Objekte enthalten sind"
      properties:
        executionDateTime:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
          description: "Das Datum ab dem im Lokationsbündel keine Objekte enthalten sind"