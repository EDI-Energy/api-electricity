components:
  schemas:
    calculationFormulaMaLoTimePeriod:
      required:
      - executionTimeFrom
      - calculationSteps
      - result
      type: object
      description: "Berechnungsformel für einen Zeitraum"
      x-Translation:
        de: "Berechnungsformel für einen Zeitraum"
      properties:
        executionTimeFrom:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
          description: "Zeitpunkt Gültig Ab"
        executionTimeUntil:
          $ref: './../dateAndTime/zeroTimeLegalGerman.yaml#/components/schemas/zeroTimeLegalGerman'
          description: "Zeitpunkt Gültig Bis"
        calculationSteps:
          $ref: './calculationStepsMaLo.yaml#/components/schemas/calculationStepsMaLo'
          description: "Rechenschritte der Berechnungsformel"
        allocationFactors:
          $ref: './allocationFactors.yaml#/components/schemas/allocationFactors'
          description: "Liste von Aufteilungsfaktor"
        parameterIDs:
          $ref: './parameterIdCalculationFormulas.yaml#/components/schemas/parameterIdCalculationFormulas'
          description: "Liste Parameter-IDs"
        result:
          $ref: './calculationFormula.yaml#/components/schemas/calculationFormula'
          description: "Die Berechnungsformel ausgeschrieben"