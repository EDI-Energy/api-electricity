components:
  schemas:
    commandControl:
      required:
      - commandControl
      type: object
      properties:
        commandControl:
          oneOf:
            - $ref: './commandControlsControllableResource.yaml#/components/schemas/commandControlsControllableResource'
            - $ref: './commandControlsNetworkLocation.yaml#/components/schemas/commandControlsNetworkLocation'
