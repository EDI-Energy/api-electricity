components:
  schemas:
    complaintDefinitionType:
      type: string
      x-Translation:
        de: "Typ der Definition die reklamiert wird"
      description: "Typ der Definition die reklamiert wird"
      example: "countingTime"
      enum:
      - countingTime
      - powerCurve
      - switchingTime