components:
  schemas:
    resultPositive:
      description: 
      required:
      - statePositive
      type: object
      properties:
        statePositive:
          $ref: './statePositive.yaml#/components/schemas/statePositive'